<div id="<%= @place.id %>">
  <h1> <%= @place.name %> </h1>
  <p><%= @place.status %></p>
  <p><%= @place.street %></p>
  <p><%= @place.city %>, <%= @place.country %></p>
  <%= link_to "Back to places", places_path %>
</div>