<p style="color: green"><%= notice %></p>

<%= render @beer %>

<div>
  <%= link_to "Back to beers", beers_path %>
  <%= link_to "List of Breweries", breweries_path %>
  <%= link_to("Edit this beer", edit_beer_path(@beer)) unless current_user.nil? %> |
  <%= button_to("Destroy this beer", @beer, method: :delete) unless current_user.nil? %>
</div>
