<%= render @style %>

<div>
  <%= link_to("Edit this style", edit_style_path(@style)) unless current_user.nil? %> |
  <%= link_to "Back to styles", styles_path %>

  <%= button_to("Destroy this style", @style, method: :delete) unless current_user.nil? %>
</div>
